const bcrypt = require('bcrypt'); const jwt = require('jsonwebtoken'); const mysql = require('mysql2/promise'); async function test() { const c = await mysql.createConnection({host: 'localhost', user: 'root', password: '', database: 'lunch_app'}); const [rows] = await c.query('SELECT * FROM users WHERE id="30609"'); console.log('Role in DB:', rows[0].role, 'admin_id:', rows[0].admin_id); c.end(); } test();
